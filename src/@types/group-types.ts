export type groupData = {
  userId: number;
  name: string;
  description: string;
  image: string;
};

export type messageData = {
  userId: number;
  groupId: number;
  message: string;
};

export type idGroupType = {
  id: number;
};
